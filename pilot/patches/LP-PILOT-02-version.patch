--- a/fs/proc/version.c
+++ b/fs/proc/version.c
@@ -8,10 +8,8 @@
 
 static int version_proc_show(struct seq_file *m, void *v)
 {
-	seq_printf(m, linux_proc_banner,
-		utsname()->sysname,
-		utsname()->release,
-		utsname()->version);
+	seq_printf(m, "LP-PILOT-02 patched-by-harness!\n");
+	seq_putc(m, '!');
 	return 0;
 }
 
