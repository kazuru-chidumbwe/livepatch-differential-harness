# Lab runbook — PRE + modest CVE (cite-pin v0.2.0)

**Executed 3 Sep 2026** on host `boma-test` (`172.19.4.243`, user `boma`).  
**Not** Tailscale Lab Test Server `100.110.54.48` (that pin was down from the desk that day).

**Close markers**

| Marker | Value |
| --- | --- |
| `DIRTYPIPE_PREGATED_DONE` | `2026-09-03T19:55:28Z` |
| `V020_DONE` | `1` |
| Dirty Pipe bzImage SHA-256 | `dccbb329c49374f41fd5960711d80b4c039f2e1c37ed4e6bc5a9869feaef338b` |

```bash
# On the execution host (harness tree as cloned):
H=$HOME/Project-Atlas/repos/livepatch-differential-harness   # or /opt/atlas/repos/...
export WORK_ROOT=$HOME/livepatch-pilot
export DIRTYPIPE_LINUX=/opt/atlas/livepatch-corpus/linux-dirtypipe
export DIRTYPIPE_BZ=$DIRTYPIPE_LINUX/arch/x86/boot/bzImage
unset CURDIR
python3 "$H/pilot/scripts/test_pre_revert_scan.py"
bash "$H/pilot/scripts/17-run-v020.sh"   # CVE packs + Dirty Pipe PRE-gated QEMU
# Dirty Pipe only (bzImage already built):
bash "$H/pilot/scripts/21-run-dirtypipe-pregated.sh"
```

Handbuild `Makefile` for Dirty Pipe uses `THIS_DIR` from `MAKEFILE_LIST`. Do not invoke `17-run-v020.sh` with an exported `CURDIR` from a kernel `make`, and do not leave the shell `cd`’d into the v5.16.10 tree (`M=` must be the handbuild directory).

## Packs closed this run

- INCLUDE contract: `pilot/results/LP-CVE-2023-52577/`, `LP-CVE-2024-36904/`
- DIRTYPIPE PRE-gated: `pilot/results/LP-CORPUS-DIRTYPIPE/` (`DIRTYPIPE_PREGATED_DONE.txt`)

Git tag `v0.2.0` is still the cite-pin freeze (author tag + push). Workstation: do not use local Docker Desktop for these QEMU packs.
